BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[UserInfo] (
    [id] INT NOT NULL IDENTITY(1,1),
    [name] NVARCHAR(1000) NOT NULL,
    [pin] NVARCHAR(1000) NOT NULL,
    [email] NVARCHAR(1000),
    [pintwo] NVARCHAR(1000) NOT NULL,
    CONSTRAINT [UserInfo_pkey] PRIMARY KEY CLUSTERED ([id]),
    CONSTRAINT [UserInfo_pin_key] UNIQUE NONCLUSTERED ([pin]),
    CONSTRAINT [UserInfo_pintwo_key] UNIQUE NONCLUSTERED ([pintwo])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
